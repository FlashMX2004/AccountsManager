<UserControl x:Class="FMX.AccountsManager.PasswordBlockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dm="clr-namespace:FMX.AccountsManager.Design" 
             xmlns:local="clr-namespace:FMX.AccountsManager"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="500">
    <Grid d:DataContext="{x:Static dm:AccountRecordDesignModel.Instance}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Label}"
                   Grid.Row="0"
                   FontSize="{StaticResource FontSizeRegular}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   />

        <ItemsControl ItemsSource="{Binding Fields}"
                      Grid.Row="1"
                      >
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:AccountRecordFieldView/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </Grid>
</UserControl>
