<UserControl x:Class="FMX.PasswordManager.PasswordBlockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dm="clr-namespace:FMX.PasswordManager.DesignModels" 
             xmlns:local="clr-namespace:FMX.PasswordManager"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="500">
    <Grid d:DataContext="{x:Static dm:PasswordBlockDesignModel.Instance}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Label}"
                   Grid.Row="0"
                   FontSize="{StaticResource FontSizeRegular}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   />

        <ItemsControl ItemsSource="{Binding Records}"
                      Grid.Row="1"
                      >
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:PasswordBlockRecordView/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </Grid>
</UserControl>
